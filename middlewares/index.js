const authMiddlewar = require("./authMiddlewar");
const upload = require("./fileMiddlewar");
const validationMiddleware = require("./validationMiddleware");

module.exports = {
  authMiddlewar,
  upload,
  validationMiddleware,
};
